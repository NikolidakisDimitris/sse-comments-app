rabbitmq:
  address: rabbitmp:5672
#  port: 5672
#  host: rabbitmq
  exchanges:
    view: view.fan
    receiver: receiver.topic
  queues:
    receiver: receiver.queue
    view: view.queue
  routing-keys:
    receiver: receiver.routing-key
